%************************************************
\chapter{Methodology}\label{cap:methodology}
%************************************************

% switch between these depending on page size, or modify directly
% same for the rest of the chapters
\tikz[remember picture,overlay] \node[opacity=0.3,inner sep=0pt] at (current page.center){\includegraphics[width=\paperwidth,height=\paperheight]{./Figures/cover/sesshu_1.jpg}};
%\tikz[remember picture,overlay] \node[opacity=0.3,inner sep=0pt] at ([yshift=3cm,xshift=2cm]current page.center){\includegraphics[width=\paperwidth,height=\paperheight]{./Figures/cover/sesshu_1.jpg}};
\clearpage 

Lorem ipsum

\section{Simulation environment}

Lorem ipsum hablar d sumo etcs

q usamos jupedsim o want to, el collision free speed model \cite{Tordeux2015}
q queremos usar el Realistic Bycicle Dynamics Model \cite{Ostendorf2025}

\section{Scenario design}

Fusilar este paper!!! \cite{Ostendorf2025b}

\subsection{Demand generation}
\subsubsection{Data sources}
Traffic count data was generated by the rom the COMPASS system \cite{Paag2019}, explicar columnas etc, corresponding
 to \ac{AADT} etc. The data generated corresponds to the model's base
  calculation for the year 2025.

The \ac{AADT} count data was converted to peak hour traffic volumes using
  a peak hour $k$ factor so that

\begin{equation}
    \text{Peak hour volume} = k \cdot \text{AADT}
\end{equation}
  
  The \ac{FHWA}
  states that in urban areas the $k$ value is placed towards the lower end of the $0.07-0.12$ range \cite{FHWA2018}.
  To obtain a better estimate of the factor, the \ac{AADT} values for several
    locations obtained with COMPASS were assessed against the peak hours observed from actual count data for the
    same locations. A $k$ factor of $0.075$ was found to provide close enough estimates for the different modes.\footnote{
        The counts assessed correspond to the ones reported in \cite{kk_Dronning}, \cite{kk_Gothersgade}, \cite{kk_Gyldenlovesgade}, \cite{kk_Mollegade}, \cite{kk_Norre}, \cite{kk_Peblinge}, \cite{kk_Slotsgade} and \cite{kk_Tagensvej}.
    }
% DECIR Q NETWORK no tiene cruces de peatones mas q los pintaos pero q en calles interiores peatones cruzan a su bola y dificil modelar

% decir q hemos jutnado intersecciones proximas con semaforos pareados en signal groups xq
%  si no los modelos se volvian locos q no haboa espacio para modelar bien etc vease results de idqn en 420
% decir q hemos diseñado fases en SUMO manualmente basadas en reales xq las por defecto volvian loco
% a modelo; en todas, giros a dchas de bicis estan en green minor para repre como ceden paso a peats


\section{Reinforcement learning framework}

Decir q se ha usado el framework de RESCO, explicar estados etc

\subsection{Handling of vehicles and pedestrians that never reach their destination}

During the different development phases, it was observed that part of the
 vehicles and pedestrians simulated never reached their destinations, sometimes
 simply because the simulation ended while they were on-route, but in most of
 the cases as a result of becoming stuck due to traffic congestion.

While several fixes in scenario design resulted in significant improvements in
 this regard, a non-negligible amount of simulated persons still never reached
 their destinations, especially when agents performed poorly or where in early
 training stages.

The reward function used by the IDQN algorithm (see tal) analyses at each step
 the \verb|total_wait| value, and thus it quickly became monotonic as the
 stuck vehicles kept accumulating time and thus penalising the agent for
 failures it could no longer fix. To address this, a different reward function
 was formulated, using the delta of the \verb|total_wait| value between steps.
\begin{equation}
    r_t = \verb|total_wait|_{t-1} - \verb|total_wait|_t
\end{equation}

% DEFINIR una vez tengamos bien

To normalize the reward values, the positive 95\% percentile of the \verb|total_wait|
 delta values observed during the training process was chosen, along with a clipping
 to the range $[-p, p]$, where experimentation determined 3 as the best value for $p$:
 
% poner ec

replay-buffer reset hook for IDQN agents and wired it to run at the start of each training episode, so the buffer clears every epoch


\section{Definitions and architecture}

Lorem ipsum